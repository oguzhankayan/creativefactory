import { TextRevealByWord } from "./ui/text-reveal-by-word";

export default function RevealStatement() {
  return (
    <TextRevealByWord text="On yıl büyük markaların işinde çalıştım. Şimdi aynı işi tek başıma yapıyorum. Arada kimse yok." />
  );
}
